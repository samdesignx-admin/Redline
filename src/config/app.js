const SITE_URL = "https://uxnest.ai";

const SCREEN_LIMIT = 5;
const NAV_LIMIT = 5;
const AUDIT_QUOTA = 1; // one complete audit included per account
const AUDIT_PRICE_USD = 5;\nconst QUOTA_MESSAGE = "Your free audit has been used. Purchase another audit for $5.";

export { SITE_URL, SCREEN_LIMIT, NAV_LIMIT, AUDIT_QUOTA, AUDIT_PRICE_USD, QUOTA_MESSAGE };
